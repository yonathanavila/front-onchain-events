"use client";
import { Card, Col, Row, Typography, Input } from "antd";

export default function AttendEvent() {

    const { Title } = Typography;

    return (
        <>
            <Title level={1}>Grande de Grandes</Title>
        </>
    );
}